"""Pure data, copied verbatim from the real SilverCity source -- only the
entries actually used by codeintel3's copied (Python-only) file set."""

python_keywords = \
    "False None True " \
    "and as assert break class continue def del elif else except " \
    "exec finally for from global if import in is lambda not or pass print " \
    "raise return try while with yield"
